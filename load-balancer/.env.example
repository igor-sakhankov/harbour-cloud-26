# Backend instances to load-balance across (comma-separated). Required.
LB_BACKENDS=http://localhost:8081,http://localhost:8082,http://localhost:8083

# Port the load balancer listens on.
PORT=8080

# How often (ms) to probe each backend's health.
HEALTH_INTERVAL_MS=10000
